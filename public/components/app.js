const app = angular.module('app', []);

app.directive('app', function () {
  return {
    scope: {},
    controller: function () {},
    controllerAs: 'ctrl',
    bindToController: true,
    templateUrl: '',
  };
});

// app.service('igdb', function($) {
//
// })
